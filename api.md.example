# jtptllm compatible-mode 接口文档 (复制为 api.md 后填入实际地址)
# cp api.md.example api.md
#
# 启动: docker compose up -d
#   需将 docker-compose.yml 中的 volume 改为:
#   - ./api.md:/app/api.md:ro
#
# 或直接: ./jtptllm-proxy --api-md api.md

key:

## 接口调用地址

```http
POST http://<GATEWAY_HOST>:<PORT>/xlm-gateway--vinrl/sfm-api-gateway/gateway/compatible-mode/v1/chat/completions
```
